services:
  - type: web
    name: mcp-code-review-buddy
    runtime: docker
    dockerfilePath: Dockerfile.render
    plan: free
    envVars:
      - key: REPO_ROOT
        value: /app
      - key: PYTHONPATH
        value: /app
      - key: DANGEROUSLY_OMIT_AUTH
        value: "true"

