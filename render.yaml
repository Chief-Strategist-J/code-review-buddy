services:
  - type: web
    name: mcp-code-review-buddy
    runtime: docker
    dockerfilePath: Dockerfile.render
    plan: free
    healthCheckPath: /health
    envVars:
      - key: REPO_ROOT
        value: /app



